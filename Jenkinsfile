#!/usr/bin/env groovy

//déclaration de la shared library build_libs
@Library(['cots_libs']) _

snapshot_gradle{
    DOCKER_BUILD = true
    IMAGE_BUILD_VERBOSE = true
    ANSIBLE_MODULE_NAMES = ["version_dck_sb_atst"]
}
